[project]
name = "mlflow-selfhosting"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-docker-build>=0.0.22",
    "pulumi-gcp>=9.0.0,<10.0.0",
    "pulumi-neon",
]

[tool.uv.sources]
pulumi-neon = { path = "sdks/neon" }

[project]
name = "pulumi-mlflow-selfhosting"
version = "0.1.0"
description = "Pulumi plugin to deploy a minimal-cost MLflow environment on GCP + Neon"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.260.0",
    "pulumi-docker-build>=0.0.22",
    "pulumi-gcp>=9.35.1",
    "pulumi-neon",
]

[tool.uv.sources]
pulumi-neon = { path = "sdks/neon" }
